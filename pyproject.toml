[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-atlas-trust-audit"
version = "0.1.0"
description = "Local, deterministic claim-evidence audit primitives."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["atlas_engine"]

[tool.setuptools.package-data]
atlas_engine = ["*.json"]
